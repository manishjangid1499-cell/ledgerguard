spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

ledgerguard:
  outbox:
    publisher:
      enabled: false
  security:
    cookie:
      secure: false

logging:
  level:
    com.ledgerguard: INFO
    org.springframework.web: WARN
    org.springframework.security: WARN
