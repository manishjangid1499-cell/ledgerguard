spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

ledgerguard:
  security:
    cookie:
      secure: false

logging:
  level:
    com.ledgerguard: INFO
    org.springframework.web: WARN
    org.springframework.security: WARN
