# LedgerGuard Failure Model & Mitigation Strategy

This document details potential failure scenarios in distributed financial systems, the theoretical risks they introduce, the required system behavior to preserve correctness, and the corresponding implementation phase in the build plan.

---

## Failure Matrix

| # | Failure Scenario | What Can Go Wrong | Expected System Behavior | Addressed In |
|---|------------------|-------------------|--------------------------|--------------|
| **1** | **Duplicate API Request** | Client retries `POST /transfers` or `POST /payments` due to client-side network glitch, causing double debits. | The idempotency engine matches `(client_id, idempotency_key)`, verifies SHA-256 request fingerprint, and returns the cached result without executing financial side effects. | **Phase 9** *(Idempotency)* |
| **2** | **Concurrent Spending (Double-Spend)** | Two simultaneous transfer requests exceed available balance when executed concurrently in separate threads. | Deterministic row-level locking (`PESSIMISTIC_WRITE` on `ledger_balance_snapshots` ordered by `ledger_account_id ASC`) serializes balance checks and updates; the second transaction observes the updated locked balance, detects insufficient funds, and throws `InsufficientFundsException`. The transaction rolls back cleanly (0 transfers, 0 journals, uncommitted idempotency claim dropped), leaving the idempotency key unpoisoned for safe retry after future funding. | **Phase 10 & 11** *(Transfers, Concurrency & Overdraft Prevention)* |
| **3** | **Deadlock Risk in Opposing Transfers** | Thread A transfers from Account 1 to Account 2 while Thread B transfers from Account 2 to Account 1 simultaneously. | Accounts are locked in a deterministic global order (`ORDER BY ledger_account_id ASC`) regardless of transfer direction, eliminating circular-wait conditions for touched accounts. Transient database exceptions propagate to caller as clean rollbacks without hidden automatic retry loops; callers can safely retry idempotent requests. | **Phase 11** *(Deterministic Locking)* |
| **4** | **Application Crash Before DB Commit** | Server process crashes or loses power mid-transfer execution before PostgreSQL transaction commit. | PostgreSQL rolls back all uncommitted changes; no partial ledger entries, balances, or outbox records exist. System state remains strictly consistent. | **Phase 7 & 10** *(ACID Posting & Transfers)* |
| **5** | **Application Crash After DB Commit** | Transaction commits in PostgreSQL, but server crashes before returning HTTP 200 to client. | Financial state and outbox events are already committed. When the client retries with the same `Idempotency-Key`, the system retrieves the committed result and returns it. | **Phase 9 & 16** *(Idempotency & Outbox)* |
| **6** | **Lost HTTP Response** | Transaction completes successfully, but network drop prevents client from receiving HTTP response. | Client retries with identical `Idempotency-Key`. The idempotency layer recognizes the completed transaction and returns the original response without re-executing. | **Phase 9** *(Idempotency)* |
| **7** | **Kafka Unavailable** | Kafka cluster is temporarily unreachable when financial transactions occur. | Financial transactions commit successfully to PostgreSQL along with pending outbox events. Outbox publisher retries delivery with exponential backoff until Kafka recovers. | **Phase 16 & 17** *(Transactional Outbox & Kafka)* |
| **8** | **Outbox Publisher Crash** | Outbox publisher process crashes mid-poll or mid-publish to Kafka. | Events remain in `PENDING` state with lease timestamps. Another instance or restarted publisher claims them via `SKIP LOCKED` and resumes publishing. | **Phase 17** *(Outbox Publisher)* |
| **9** | **Kafka Duplicate Delivery** | Kafka at-least-once delivery semantics cause the same event message to be delivered twice to workers. | The consumer's inbox deduplication mechanism (`processed_events` table) detects duplicate event ID within the consumer database transaction and ignores it. | **Phase 18** *(Notification Worker Inbox)* |
| **10** | **Consumer Crash / Redelivery** | Notification worker crashes after processing business effect but before committing Kafka offset. | On restart, Kafka redelivers the message; the consumer's inbox table recognizes the `event_id` as already processed and skips duplicate side effects. | **Phase 18** *(Inbox Deduplication)* |
| **11** | **PSP Failure Before Processing** | External PSP returns HTTP 500 or network error before processing deposit or withdrawal. | Transaction transitions to `FAILED`; any active balance holds are immediately released. No journal entries are posted. | **Phase 19, 20 & 21** *(PSP Integration & Holds)* |
| **12** | **PSP Timeout Before Processing** | Call to PSP times out before the PSP receives or processes the request. | Transaction transitions to `UNKNOWN` or `PROCESSING`. System initiates provider status query or awaits webhook before declaring state. Funds remain held. | **Phase 23** *(Ambiguous Outcomes)* |
| **13** | **PSP Timeout After Successful Processing** | PSP successfully commits payment/payout, but HTTP response times out on the return path. | Transaction is marked `UNKNOWN` / `RECONCILIATION_REQUIRED`. Funds are not prematurely refunded. Status poller or reconciliation syncs state with PSP. | **Phase 23 & 24** *(Lifecycle & Reconciliation)* |
| **14** | **Delayed Response from PSP** | PSP takes several minutes to process an asynchronous settlement. | Transaction stays in `PROCESSING` with active balance hold. Funds cannot be spent elsewhere until final webhook or reconciliation callback arrives. | **Phase 15 & 23** *(Holds & State Machines)* |
| **15** | **Delayed Webhook Arrival** | PSP webhook arrives hours or days after the initial transaction request. | Webhook processing verifies signature and updates operation state; if already resolved via polling, the webhook is recognized as redundant and safely acknowledged. | **Phase 22 & 23** *(Webhooks & State Machines)* |
| **16** | **Duplicate Webhook Delivery** | PSP sends the same webhook payload multiple times. | Webhook signature is validated and inbound `provider_events` table enforces unique constraint on `(provider_id, provider_event_id)`. Duplicates are acknowledged without action. | **Phase 22** *(Webhook Deduplication)* |
| **17** | **Out-of-Order Webhook Delivery** | Webhook for `SUCCEEDED` arrives before initial `CREATED` notification or vice versa. | State machine enforces legal state transitions (`CREATED` $\to$ `PROCESSING` $\to$ `SUCCEEDED`). Out-of-order transitions are evaluated against current state and applied safely. | **Phase 22 & 23** *(State Machine Guards)* |
| **18** | **Stale Provider State Update** | An outdated status callback arrives after an operation was already marked `RESOLVED` or `SUCCEEDED`. | Terminal states (`SUCCEEDED`, `FAILED`, `REFUNDED`) are immutable. The stale update is logged and rejected by the state machine validator. | **Phase 23** *(State Transitions)* |
| **19** | **Payment Insufficient Funds** | Customer attempts payment where available balance < gross amount. | Validated against locked customer snapshot before journal posting; transaction rolls back cleanly. No `Payment` row is committed, no journal entries exist, and the uncommitted idempotency slot is dropped, allowing safe retry with the same key after wallet funding. | **Phase 13** *(Merchant Payments)* |
| **20** | **Payment Fee Account Ambiguity / Misconfiguration** | Payment with fee > 0 encounters missing, multiple, or closed `PLATFORM_FEES` accounts. | Throws `PlatformFeeAccountException`; the entire transaction rolls back cleanly. No payment or journal committed; no silent loss or arbitrary fee account selection. | **Phase 13** *(Merchant Payments)* |
| **21** | **Payment Snapshot Arithmetic Overflow** | Posting payment causes account balance to exceed signed 64-bit integer limit. | PostgreSQL trigger detects arithmetic overflow and raises exception; transaction rolls back completely. 0 payment rows, 0 journals, 0 committed idempotency records. | **Phase 8 & 13** *(Balance Snapshots & Payments)* |
| **22** | **Concurrent Refund Requests & Over-Refunds** | Multiple simultaneous refund requests attempt to refund more than the original payment amount. | Parent `Payment` row is locked `FOR UPDATE` before cumulative refund calculation. Application and DB trigger enforce $\sum \text{Refunds} \le \text{grossAmountMinor}$. Requests exceeding the cap fail with HTTP 409 `REFUND_LIMIT_EXCEEDED` and roll back cleanly (0 refund rows, 0 journals, uncommitted idempotency slot dropped for safe retry). | **Phase 14** *(Refunds & Concurrency Control)* |
| **23** | **Refund on Non-SUCCEEDED or Unowned Payment** | Client attempts to refund an unowned, missing, `CREATED`, `PROCESSING`, or `FAILED` payment. | Unowned or non-existent payments return HTTP 404 (IDOR / anti-enumeration protection). Non-succeeded payments fail with HTTP 409 `PAYMENT_NOT_REFUNDABLE` and DB trigger enforces `payment.status = 'SUCCEEDED'`. Entire transaction rolls back cleanly. | **Phase 14** *(Refunds & Integrity)* |
| **24** | **Refund Fee Account Resolution / Journal Integrity Failure** | Original payment journal has missing, ambiguous, or multiple platform fee credit entries. | Throws `PlatformFeeAccountException` causing immediate rollback. Does not guess or fallback to current repository fee accounts; original immutable journal determines exact fee reversal. | **Phase 14** *(Refunds & Accounting Integrity)* |
| **25** | **Refund Balance Snapshot Overflow** | Compensating customer credit causes customer snapshot balance to exceed signed 64-bit integer range. | Trigger `trg_journal_transactions_update_snapshots` detects overflow and raises exception; transaction rolls back completely (0 refund rows, 0 journals, 0 committed idempotency records, unmutated balances). | **Phase 8 & 14** *(Balance Snapshots & Refunds)* |
| **28** | **Concurrent Over-Reservation (Hold Double-Spend)** | Multiple simultaneous hold requests attempt to reserve more funds than available in the wallet balance. | Parent snapshot row is locked `FOR UPDATE` in `ORDER BY ledger_account_id ASC`. Application and database trigger enforce $\sum \text{ACTIVE holds} + \text{newAmount} \le \text{postedBalance}$. Requests exceeding available funds fail with `InsufficientAvailableBalanceException` and roll back cleanly (0 hold rows created, unmutated balances). | **Phase 15** *(Balance Holds)* |
| **29** | **Hold vs. Spending Cross-Operation Race** | Client attempts to create a balance hold and execute a transfer/payment concurrently on the same funds. | All reservation and spending operations coordinate through deterministic `PESSIMISTIC_WRITE` locks on the wallet's `ledger_balance_snapshots` row. Both paths evaluate against `availableBalance = postedBalance - sum(ACTIVE holds)`. Mutual exclusion guarantees only one operation succeeds when combined funds exceed total balance. | **Phase 15** *(Balance Holds & Available-Balance Model)* |
| **30** | **Hold Expiration Race with Release / Consumption** | Scheduled background expiration worker attempts to expire a hold concurrently with an operation consuming or releasing it. | Multi-instance safe conditional update (`UPDATE ... WHERE id = :id AND status = 'ACTIVE'`) and trigger-enforced terminal status immutability guarantee exactly one terminal transition succeeds, preventing duplicate or contradictory lifecycle state changes. | **Phase 15** *(Hold Expiration & Lifecycle)* |
| **31** | **Balance Snapshot Corruption / Drift** | Hardware fault, direct DB tampering, or software bug causes `account_balances` to deviate from ledger sum. | Balance reconciliation detects the discrepancy; automated/manual repair recalculates the exact balance from immutable journal entries and corrects the snapshot. | **Phase 24 & 25** *(Reconciliation & Recovery)* |
| **32** | **Reconciliation Mismatch with PSP** | LedgerGuard records a transaction as `FAILED`, but PSP settlement report indicates `SUCCEEDED`. | Reconciliation engine flags a discrepancy item (`AMOUNT_MISMATCH` or `STATUS_MISMATCH`) and routes it to the `MANUAL_REVIEW` queue for operator intervention. | **Phase 24 & 25** *(Reconciliation Engine)* |
| **33** | **Business Failure Before Outbox Append (Scenario A)** | Financial validation fails or balance is insufficient during transfer/payment/refund before outbox event is appended. | The entire transaction rolls back cleanly in PostgreSQL; 0 business records, 0 journal transactions, 0 journal entries, 0 outbox event rows committed. | **Phase 16** *(Transactional Outbox Persistence)* |
| **34** | **Outbox Serialization or Persistence Failure (Scenario B)** | Outbox event payload serialization fails or database constraint on outbox table is violated during first execution. | The entire transaction rolls back via `Propagation.MANDATORY`; business state does NOT commit, no partial financial writes or unbacked transfers/payments exist, and idempotency slot is dropped for safe retry. | **Phase 16** *(Transactional Outbox Persistence)* |
| **35** | **Process Crash Post-DB Commit (Scenario C)** | Business operations, financial records, idempotency completion, and PENDING outbox event commit, but JVM process crashes before responding to HTTP client or publishing event. | All financial and outbox state remains durable in PostgreSQL. The PENDING outbox event represents undelivered intent and is safely picked up and published by the asynchronous publisher worker in Phase 17 upon recovery. | **Phase 16 & 17** *(Transactional Outbox Durability & Recovery)* |
| **36** | **Kafka Broker Send Failure / Unavailability** | Kafka cluster is temporarily unreachable or broker rejects send attempt. | `OutboxPublisherService` catches failure, throws `OutboxPublishException`, and rolls back the database transaction. Outbox row remains `PENDING` with `published_at = NULL`; subsequent scheduled publisher cycles retry automatically. | **Phase 17** *(Outbox Publisher Reliability)* |
| **37** | **Kafka Send Timeout / Ambiguous Outcome** | Broker ACK wait times out on `future.get(timeout)`; publisher cannot determine if message reached broker. | Publisher treats timeout as ambiguous, rolls back DB transaction leaving row `PENDING`. On subsequent retry, message is republished with the identical stable event ID (`outbox_events.id`), enabling idempotent deduplication at consumer inboxes. | **Phase 17 & 18** *(Ambiguous Delivery & Inbox Dedup)* |
| **38** | **Crash Post-Kafka ACK Before DB Commit** | Broker accepts message and sends ACK, but application process crashes or loses DB connectivity before committing `PUBLISHED` status. | Uncommitted DB changes roll back; outbox row remains `PENDING`. Upon restart, publisher worker repolls row and publishes it again with identical stable event ID, achieving safe at-least-once delivery. | **Phase 17 & 18** *(At-Least-Once Delivery & Idempotent Redelivery)* |
| **39** | **PSP Transport Failure Before Acceptance (`TEMPORARY_500`)** | External PSP returns HTTP 500 on first $N$ requests before accepting the transaction. | Deterministic fault injection produces HTTP 500 without creating provider operation or webhook rows (0 DB rows). Caller treats this as a known failure and can safely retry until success. | **Phase 19** *(PSP Simulator)* |
| **40** | **PSP Timeout After Success (`TIMEOUT_AFTER_SUCCESS`)** | Provider transaction commits successfully, but network delay/loss causes caller to timeout waiting for HTTP response. | Provider operation is committed as `SUCCEEDED` in the database *before* the delay occurs. Subsequent status lookup (`GET /api/provider/operations/by-client/{clientOperationId}`) confirms success; retry returns existing operation with 200 OK without re-executing. | **Phase 19 & 23** *(PSP Ambiguity & Status Recovery)* |
| **41** | **Delayed Webhook Delivery (`DELAYED_WEBHOOK`)** | Webhook callback is delayed in transit or queued for asynchronous delivery. | Operation succeeds immediately and returns 201 Created. Webhook is scheduled for `now + delayMs` and dispatched by background worker once due. | **Phase 19** *(PSP Simulator)* |
| **42** | **Duplicate Webhook Delivery (`DUPLICATE_WEBHOOK`)** | External network or provider delivers the same logical webhook callback multiple times. | Provider schedules 2 separate delivery rows carrying the exact same `eventId` and payload. Downstream receivers must use event ID deduplication to process side effects idempotently. | **Phase 19 & 22** *(Duplicate Webhook Delivery)* |
| **43** | **Webhook Target Endpoint Failure** | Downstream webhook callback URL is unreachable, times out, or returns HTTP 5xx. | Webhook dispatcher marks the delivery row as `FAILED` without failing or rolling back the `SUCCEEDED` provider operation. Financial and provider state remain authoritative. | **Phase 19** *(Webhook Decoupling)* |
| **44** | **Conflicting Provider Operation Replay** | Same `clientOperationId` is resubmitted with modified parameters (different amount, currency, operation type, or webhookUrl). | Provider checks request fingerprint against existing record and rejects conflicting retry with HTTP 409 `Conflict`. Existing operation is not overwritten. | **Phase 19** *(Provider Idempotency)* |
| **45** | **PSP Unconfirmed on Wallet Funding (`TEMPORARY_500` / Timeout)** | External PSP call times out or returns 5xx error during customer wallet funding. | `FundingService` catches transport/timeout exception and preserves `FundingOperation` in `PROCESSING` status with 0 wallet credit (returns HTTP 202 Accepted). Subsequent retry with same `Idempotency-Key` safely replays the call using the existing `clientOperationId`. | **Phase 20** *(External Wallet Funding)* |
| **46** | **PSP Response Validation Mismatch** | External PSP returns a response with mismatched currency, operation type, amount, or clientOperationId. | `FundingSettlementService` validates all response fields; on mismatch, it aborts settlement without updating balances. `FundingOperation` remains `PROCESSING` and no unbacked ledger credit occurs. | **Phase 20** *(Funding Integrity & Response Validation)* |
| **47** | **Local Settlement Failure Rollback** | External PSP succeeded, but local double-entry journal posting or snapshot lock fails. | Database transaction rolls back cleanly; `FundingOperation` remains `PROCESSING` and 0 customer credit is committed. Subsequent retry can safely settle the confirmed provider operation. | **Phase 20** *(Funding Settlement Atomicity)* |
| **48** | **PSP Clearing Account Misconfiguration** | Funding settlement encounters 0 or $>1$ active `PSP_CLEARING` accounts. | Throws `PspClearingAccountException`, rolling back local settlement. FundingOperation remains `PROCESSING` with 0 ledger credit; fails closed instead of selecting arbitrary clearing accounts. | **Phase 20** *(Clearing Account Integrity)* |
| **49** | **Payout PSP Definite Failure (`TEMPORARY_500` / `FAILED`)** | External PSP returns definite 500 error or `FAILED` response during outbound payout. | `PayoutFailureService` releases the reserved `BalanceHold` (`RELEASED`), marks payout `FAILED`, with 0 journal entries. Customer/Merchant spendable available balance is fully restored with 0 double-spend or money destruction. | **Phase 21** *(External Payouts)* |
| **50** | **Payout PSP Timeout / Ambiguous Network Outcome** | External PSP call times out or encounters network connection reset during payout. | `PayoutService` preserves payout in `PROCESSING` status and keeps `BalanceHold` `ACTIVE`, returning HTTP 202 Accepted. Zero money is deducted from posted balance; hold prevents double-spend until authoritative resolution. | **Phase 21** *(External Payouts & Ambiguity)* |
| **51** | **Payout In-Flight Hold Expiration Premature Cancellation Race** | Generic background hold expiration worker runs while an external payout is in flight. | Hold expiration query explicitly filters out holds linked to `PROCESSING` payouts (`WHERE p.id IS NULL OR p.status != 'PROCESSING'`). In-flight money remains securely reserved. | **Phase 21** *(Payout Hold Protection)* |
| **52** | **Payout Response Validation Mismatch** | External PSP returns a response with mismatched currency, operation type, amount, or clientOperationId. | `PayoutSettlementService` validates all fields; on mismatch, throws `PayoutValidationException`, leaving payout `PROCESSING` and hold `ACTIVE` without executing unvalidated ledger postings. | **Phase 21** *(Payout Integrity)* |
| **53** | **Payout Clearing Account Misconfiguration** | Payout settlement encounters 0 or $>1$ active `PSP_CLEARING` accounts. | Throws `PspClearingAccountException`, rolling back local settlement. Payout remains `PROCESSING` with hold `ACTIVE`; fails closed. | **Phase 21** *(Clearing Account Integrity)* |
| **54** | **Webhook Missing / Tampered HMAC Signature** | Attacker or corrupted transmission sends webhook with missing, invalid, or forged signature. | `HmacSignatureValidator` rejects request before opening DB transaction; returns HTTP 401 Unauthorized (`PROVIDER_AUTHENTICATION_FAILED`). Secret material is never echoed or exposed. | **Phase 22** *(Webhook Authentication)* |
| **55** | **Webhook Expired / Replayed Request** | Captured legitimate webhook replayed outside allowable tolerance window ($\pm 300\text{s}$). | Timestamp validation detects skew using overflow-safe UTC `Instant` arithmetic and rejects with HTTP 401 Unauthorized. | **Phase 22** *(Replay Protection)* |
| **56** | **Webhook Out-of-Order Sequence Arrival** | Later sequence number arrives before an earlier one (e.g. sequence 2 arrives before sequence 1). | Event is stored durably as `PENDING` via `provider_events`. Ordered cursor halts on sequence gap and returns HTTP 202 Accepted. Zero money is moved until missing sequence numbers arrive and contiguous processing unlocks the gap. | **Phase 22** *(Ordered Sequence Cursor)* |
| **57** | **Webhook Status Regression** | Webhook reports a regressive provider status (e.g. `SUCCEEDED` $\to$ `PROCESSING` or `FAILED` $\to$ `SUCCEEDED`). | Processing cursor detects illegal regression against `lastKnownProviderStatus`, marks event `IGNORED` with non-null `processed_at`, advances cursor, and executes zero financial mutations or journals. | **Phase 22** *(Regression Ignoring)* |
| **58** | **Webhook Sequence Ownership Conflict** | Incoming webhook attempts to register an existing sequence number for the same `providerOperationId` with a different `eventId`. | Classification detects sequence ownership conflict, marks transaction rolled back, and returns HTTP 409 Conflict (`PROVIDER_EVENT_CONFLICT`). No overwrites or split-brain sequences permitted. | **Phase 22** *(Conflict Classification)* |
| **59** | **Pre-Phase-22 Ambiguous Operation Replay With Webhook URL** | Pre-Phase-22 `PROCESSING` operation originally created with `webhookUrl = null` is replayed with a non-null `webhookUrl`. | Provider simulator compares request fingerprint against existing record; differing `webhookUrl` triggers provider identity conflict (HTTP 409 Conflict). Historical `SUCCEEDED` operations remain unaffected. Phase 22 replays must use the same `webhookUrl` as provider identity. Resolving pre-Phase-22 ambiguous `PROCESSING` operations with null `webhookUrl` is deferred to reconciliation (Phase 23); zero V13 or column hacks introduced. | **Phase 22** *(Provider Identity Compatibility)* |
| **60** | **Conflicting Provider Operation for Same Client ID** | Two distinct provider operations ($A \ne B$) arrive claiming settlement for the same local `clientOperationId`. | Row-level locking on `FundingOperation` or `Payout` (`FOR UPDATE` with entity refresh) serializes settlement. The winning provider operation establishes authoritative settlement; the losing operation detects conflicting `providerOperationId` and throws `ProviderEventConflictException` (HTTP 409 Conflict). Event remains non-applied; total journals $\le 1$; wallet effect strictly once. | **Phase 22** *(Cross-Provider Stream Conflict)* |
| **61** | **Ambiguous Provider HTTP 500 (Generic)** | External PSP returns HTTP 500 with a non-deterministic or missing ProblemDetail body; it is unknown whether the PSP processed the operation. | `PspClient` parses the RFC-9457 ProblemDetail body; an unrecognized or absent `type` does not qualify as a definite failure. Operation transitions to `UNKNOWN` with balance hold remaining `ACTIVE`. Zero money is lost or created. Status poller or late webhook eventually resolves. | **Phase 23** *(Ambiguous Outcome Classification)* |
| **62** | **Definite Pre-Acceptance Rejection (`urn:ledgerguard:psp:error:temporary-failure`)** | External PSP returns HTTP 500 with RFC-9457 ProblemDetail `type: urn:ledgerguard:psp:error:temporary-failure`, confirming the provider did not process the operation. | `PspClient` identifies the ProblemDetail type as machine-readable definite rejection; operation transitions `PROCESSING -> FAILED` with balance hold `RELEASED` and 0 journal entries. No ambiguity: money is not in flight. | **Phase 23** *(Definite Pre-Acceptance Failure)* |
| **63** | **Concurrent Duplicate Submission Race** | Two concurrent requests race for the same `CREATED` operation and both attempt to call the PSP. | `FundingSubmissionService`/`PayoutSubmissionService` acquires a pessimistic `FOR UPDATE` row lock. First caller claims `CREATED -> PROCESSING`; second caller observes non-`CREATED` status and returns `submissionClaimed=false`, skipping the outbound POST. Maximum 1 provider POST is guaranteed. | **Phase 23** *(At-Most-One Provider POST)* |
| **64** | **Crash Between Claim Commit and Provider POST** | Application commits `CREATED -> PROCESSING` and crashes before making the outbound HTTP POST. | Operation remains in `PROCESSING` with `next_provider_poll_at` set immediately. Background poller's Step A claims the row and queries `GET /api/provider/operations/by-client/{id}`. If provider returns 404, operation remains PROCESSING until max attempts, then transitions to `RECONCILIATION_REQUIRED`. | **Phase 23** *(Crash Recovery via Status Poller)* |
| **65** | **Provider POST Outside DB Transaction** | DB row lock is held across outbound HTTP, blocking concurrent PostgreSQL queries and risking deadlock on lock timeout. | `FundingSubmissionService` and `PayoutSubmissionService` commit the `PROCESSING` claim transaction *before* calling `PspClient.createOperation(...)`. `TransactionSynchronizationManager.isActualTransactionActive() == false` at the network boundary. | **Phase 23** *(Network Transaction Boundary)* |
| **66** | **Polling Claim Crash Between Increment and GET** | Background poller increments `provider_poll_attempts` and commits, then crashes before making the provider GET request. | `next_provider_poll_at` is advanced forward in the claim transaction. Step 0 exhaustion finalizer on the next poller cycle transitions any row with `attempts >= maxAttempts` and overdue `next_provider_poll_at` to `RECONCILIATION_REQUIRED`. No stranded rows. | **Phase 23** *(Step 0 Exhaustion Finalizer)* |
| **67** | **Poll Attempts Exhausted: Provider Perpetually PROCESSING or Unreachable** | After `maxAttempts` provider GETs, the provider still returns PROCESSING, 404, or transport error. | Step 0 finalizer detects `provider_poll_attempts >= maxAttempts` and transitions operation to `RECONCILIATION_REQUIRED` with `next_provider_poll_at = NULL`. No further polling occurs. Payout hold remains `ACTIVE` until human/automated reconciliation resolves. | **Phase 23** *(Bounded Poll Exhaustion)* |
| **68** | **Late Terminal Webhook After RECONCILIATION_REQUIRED** | After a funding or payout reaches `RECONCILIATION_REQUIRED`, an authoritative signed webhook arrives with `SUCCEEDED` or `FAILED`. | `ProviderEventProcessingService` re-reads status under lock; `RECONCILIATION_REQUIRED` is not terminal and accepts authoritative settlement. Exactly 1 journal (2 entries, balanced) is committed; payout hold is `CONSUMED` (SUCCEEDED) or `RELEASED` (FAILED). | **Phase 23** *(Late Webhook Recovery)* |
| **69** | **Conflicting Replay (HTTP 409 from Provider)** | Provider returns HTTP 409 with `urn:ledgerguard:psp:error:conflicting-replay`, indicating a request fingerprint contradiction. | `ProviderConflictTransitionService` (REQUIRES_NEW) acquires row lock, re-reads current status, and transitions non-terminal operation to `RECONCILIATION_REQUIRED` with `next_provider_poll_at = NULL` *before* returning HTTP 409. Durable state is committed before any response is propagated. | **Phase 23** *(Durable Conflict Transition)* |
| **70** | **Hold Expiration Race with UNKNOWN/RECONCILIATION_REQUIRED Payout** | Background hold sweeper attempts to expire an overdue hold linked to a payout in `UNKNOWN` or `RECONCILIATION_REQUIRED`. | `BalanceHoldRepository.findDueActiveHolds` and `expireHoldConditional` exclude holds linked to payouts in `PROCESSING`, `UNKNOWN`, or `RECONCILIATION_REQUIRED`. In-doubt holds are permanently shielded from generic expiration. | **Phase 23** *(Hold Protection for In-Doubt Payouts)* |
| **71** | **Stale CREATED Payout Hold Expiration** | A payout in `CREATED` status (never submitted to provider) has its balance hold expire naturally. | `CREATED` payouts are intentionally expirable. Hold expires, payout transitions `CREATED -> FAILED` locally with hold status `EXPIRED` and 0 provider POSTs, 0 journals. Customer available balance is fully restored. | **Phase 23** *(Stale CREATED Payout Expiry)* |
| **72** | **Provider GET Response Field Mismatch** | Status poller receives a 200 OK GET response from the provider but with mismatched `operationType`, `amount`, `currency`, or `clientOperationId`. | Settlement validation rejects the mismatched response. Operation transitions to `RECONCILIATION_REQUIRED` with 0 journals and payout hold remaining `ACTIVE`. Human/automated reconciliation must resolve. | **Phase 23** *(Provider GET Validation)* |
| **73** | **Unbalanced Historical Journal Transaction** | A historical software bug, bit flip, or operator intervention causes a `POSTED` journal's debits to differ from credits. | Level 1 `JournalBalanceChecker` aggregates debits and credits using unbounded `NUMERIC`, detects `debit_sum != credit_sum`, records `UNBALANCED_JOURNAL` `DISCREPANCY` item; 0 financial mutations made. | **Phase 24** *(Journal Balance Reconciliation)* |
| **74** | **Corrupted POSTED Journal with Zero Entries** | A corrupted historical journal exists in `POSTED` status with zero entries. | Level 1 `JournalBalanceChecker` uses `LEFT JOIN journal_entries`, detects `total_count = 0` (or missing debit/credit sides), and flags `MALFORMED_JOURNAL` `DISCREPANCY`; 0 mutation. | **Phase 24** *(Malformed Journal Detection)* |
| **75** | **Derived Snapshot Corruption / Arithmetic Drifts** | Direct DB update or bug causes `ledger_balance_snapshots.balance_minor` to diverge from authoritative posted journal history. | Level 2 `SnapshotConsistencyChecker` reconstructs posted balance in a single MVCC snapshot statement (excluding DRAFTs via subquery) and flags `SNAPSHOT_MISMATCH` `DISCREPANCY`; snapshot is NOT repaired. | **Phase 24** *(Snapshot Consistency)* |
| **76** | **Missing Balance Snapshot Row** | A ledger account exists without a corresponding row in `ledger_balance_snapshots`. | Level 2 `SnapshotConsistencyChecker` LEFT JOINs `ledger_balance_snapshots`, detects `actual IS NULL`, and logs `SNAPSHOT_MISSING` `DISCREPANCY` with expected balance; 0 repair. | **Phase 24** *(Missing Snapshot Detection)* |
| **77** | **Provider vs Local Settlement Contradiction** | Local state is `SUCCEEDED` while provider reports `FAILED` (or vice versa), risking fund loss or double settlement. | Level 3 `ProviderSettlementChecker` queries provider GET outside DB transaction, re-reads local state under lock, classifies as `PROVIDER_STATUS_MISMATCH` `DISCREPANCY`; zero financial mutation. | **Phase 24** *(Provider Settlement Audit)* |
| **78** | **Late Concurrent Discrepancy Recording During Run Finalization** | An item insert attempts to attach to a run concurrently transitioning from `RUNNING` to `COMPLETED`. | Two-phase locking serialization: `FinalizationService` holds `FOR UPDATE` on run while counting items; concurrent item insert holds `FOR SHARE`. Trigger `trg_recon_items_immutability` verifies run is `RUNNING`; once finalization commits, subsequent inserts fail trigger check. | **Phase 24** *(Reconciliation Concurrency Serialization)* |
| **79** | **Concurrent Snapshot Posting During Auto-Repair** | High-concurrency journal posting modifies snapshot balance while `SnapshotAutoRepairService` reconstructs balance. | `SnapshotAutoRepairService` locks the target `ledger_balance_snapshots` row `FOR UPDATE`. Any concurrent posting transaction serializes behind the repair lock; repaired balance reflects all committed journals, maintaining 100% mathematical consistency without overwriting new postings. | **Phase 25** *(Concurrent Posting Snapshot Repair)* |
| **80** | **Concurrent Case Claim Race Between Operators** | Two OPS operators attempt to claim the same `OPEN` discrepancy case simultaneously. | `ReconciliationCaseManagementService` uses `findByIdForUpdate` pessimistic row locking. The first operator claims `OPEN -> IN_REVIEW`; the competing operator receives HTTP 409 Conflict. Once claimed, trigger `trg_recon_cases_lifecycle` enforces null-safe immutability against reassignment or unassignment (`IS DISTINCT FROM`). | **Phase 25** *(Claim Concurrency & Immutability)* |
| **81** | **Attempted Auto-Repair of Missing Snapshot Row** | Operator triggers auto-repair on a `SNAPSHOT_MISSING` case or an account without an existing snapshot row. | Auto-repair detects `snapshotRow.isEmpty()`, aborts without inserting arbitrary rows, and returns HTTP 409 Conflict. Missing snapshot rows indicate schema or account creation faults requiring administrative investigation. | **Phase 25** *(Missing Snapshot Protection)* |
| **82** | **Manual Resolution Attempt on Snapshot Mismatch** | Operator attempts to manually resolve a `SNAPSHOT_MISMATCH` case without executing snapshot repair. | `ReconciliationCaseManagementService.resolveManually` validates item problem type and explicitly rejects `SNAPSHOT_MISMATCH` with `ReconciliationConflictException` (HTTP 409 Conflict / `RECONCILIATION_CONFLICT`). Snapshot discrepancies must undergo dynamic reconstruction via `repair-snapshot`. | **Phase 25** *(Strict Repair Boundary)* |
| **83** | **Financial Drift During Manual Review** | Operator manually marks a provider discrepancy or journal anomaly `RESOLVED`. | Manual review mutates workflow state only (`reconciliation_cases.status = RESOLVED`). Zero mutations are made to journals, entries, snapshots, holds, funding operations, payouts, or outbox events. | **Phase 25** *(Manual Review Zero Financial Mutation)* |
| **84** | **Outbound PSP Circuit Breaker Open** | Provider experiences high failure rate (>50% over sliding window), opening circuit. | Fast pre-network rejection via `CallNotPermittedException` (0 HTTP requests sent). Funding operation transitions to `FAILED` (0 ledger credit). Payout operation transitions to `FAILED` and balance hold is immediately `RELEASED` (0 money lost/locked). In Level 3 reconciliation, provider calls are skipped and recorded as `UNRESOLVED` with `problem_type = PROVIDER_UNAVAILABLE` without mutating financial records. | **Phase 26** *(Resilient Provider Client)* |
| **85** | **Outbound PSP Bulkhead Saturation** | High concurrent request volume exceeds maximum concurrency capacity (20 calls). | Fast pre-network rejection via `BulkheadFullException` without queuing or waiting (`maxWaitDuration = 0ms`). Funding operation transitions to `FAILED`. Payout operation transitions to `FAILED` with balance hold safely `RELEASED`. Status bulkhead and create bulkhead operate independently, ensuring polling or reconciliation cannot starve create operations and vice versa. | **Phase 26** *(Bulkhead Concurrency Isolation)* |
| **86** | **Authoritative Replay of `TIMEOUT_AFTER_SUCCESS`** | Initial provider POST times out in transit after provider committed `SUCCEEDED` internally. | Resilience4j retry (or subsequent idempotent status query) replays request with identical idempotency key; provider returns 200 OK with existing `SUCCEEDED` operation. State machine settles the double-entry journal, marks operation `SUCCEEDED`, and consumes balance hold. Does not remain ambiguous. | **Phase 26** *(Authoritative Replay Resolution)* |
| **87** | **Multi-Attempt Ambiguity Dominance** | An initial physical attempt times out (ambiguous outcome), but a subsequent retry returns a pre-acceptance 500 (`temporary-failure`). | The operation state machine considers all physical attempts; ambiguity dominates over subsequent pre-acceptance rejections. The logical operation transitions to `UNKNOWN` (not `FAILED`) and balance hold remains `ACTIVE` to prevent double-spend or duplicate money creation until authoritative settlement arrives. | **Phase 26** *(Multi-Attempt Ambiguity Dominance)* |
| **88** | **Poller / Reconciliation Resilience Circuit Isolation** | Status poller or Level 3 reconciliation checker encounters an open circuit breaker or saturated bulkhead. | Poller skips execution without incrementing `provider_poll_attempts`, preventing false poll exhaustion. Reconciliation Level 3 records an item with `classification = UNRESOLVED` and `problem_type = ReconciliationProblemType.PROVIDER_UNAVAILABLE` (frozen V14 schema preserved). Zero financial mutations made. | **Phase 26** *(Poller & Reconciliation Resilience Isolation)* |
| **89** | **Denial of Service / Quota Exhaustion on Financial Endpoints** | Malicious or buggy client floods `POST /api/transfers`, `POST /api/payments`, or `POST /api/funding` with rapid requests. | Rate limit filter rejects excess calls with HTTP 429 `Too Many Requests` and RFC 9457 `RATE_LIMIT_EXCEEDED` before hitting MVC controllers or DB transactions. Zero database connections acquired, zero idempotency claims created, zero ledger records mutated. Legitimate requests execute normally once quota refills. | **Phase 27** *(Rate Limiting & Admission Control)* |
| **90** | **Credential Stuffing / Authentication Flood** | Attacker floods `/api/auth/login` or `/api/auth/register` with brute-force credential attempts. | `PUBLIC_AUTH` token bucket throttles requests per client IP (10/min) returning HTTP 429 with `Retry-After` header. Authentication service and user repository are protected from CPU-intensive BCrypt hashing exhaustion. | **Phase 27** *(Public Authentication Rate Limiting)* |
| **91** | **Forbidden / Unauthorized Quota Starvation Attempt** | Attacker sends unauthorized (invalid JWT) or forbidden (unpermitted role) requests attempting to consume rate limit tokens of legitimate users. | Security filter chain enforces `JwtAuthenticationFilter` (401) and `AuthorizationFilter` (403) strictly *before* `RateLimitFilter`. Rejected calls never consume tokens; repeat 401/403 requests never yield 429. | **Phase 27** *(Security Precedence)* |
| **92** | **Worker Kafka Consumer Desynchronization / Memory Bloat** | High volume of domain events published to Kafka threatens to overwhelm worker heap memory or cause partition heartbeats to lapse during slow processing. | `notification-worker` bounds `max.poll.records = 10` and `concurrency = 3`. Consumers poll in tight, bounded batches, preserving heap stability and avoiding broker rebalances. | **Phase 27** *(Bounded Kafka Backpressure)* |
| **93** | **Attempted Tampering / Modification of Audit Log** | Malicious insider or rogue query attempts to update, delete, or truncate rows in `audit_events`. | PostgreSQL trigger `trg_audit_events_immutability` executes before statement/row mutation and aborts with explicit exception (`audit_events is append-only`). Zero modifications permitted at the database engine level. | **Phase 28** *(Immutable Audit Persistence)* |
| **94** | **Reconciliation Conflict Rollback with Partial Audit** | Administrative case claim or resolution encounters a state machine violation or concurrency conflict after audit method is called. | `AuditService` participates in the caller's transaction via `Propagation.MANDATORY`. Database transaction rolls back completely, dropping both the uncommitted case update and the audit insert. Zero orphaned or misleading audit records exist. | **Phase 28** *(Transactional Audit Atomicity)* |
| **95** | **Audit Persistence Failure During Operational Mutation** | Audit insert violates foreign key constraint, fails JSON serialization, or DB runs out of space while executing snapshot repair or manual case resolution. | Entire transaction rolls back via `Propagation.MANDATORY`. The reconciliation case and snapshot balance remain unmutated, preventing unaudited administrative interventions. | **Phase 28** *(Fail-Closed Audit Integrity)* |
| **96** | **Control Character / Script Injection in Investigation Notes** | Operator submits an investigation note containing embedded ASCII control characters (CR, LF, TAB, NUL, DEL) attempting log forging or shell script injection. | Raw string validation intercepts control characters before whitespace trimming or sanitization, throwing `InvalidReconciliationOperationException` (HTTP 400). Input is rejected before persistence. | **Phase 28** *(Input Hardening)* |
